import { bebas } from "../fonts";

export default function Title() {
  return (
    <h1 className={`${bebas.className} title text-balance w-[784px]`}>
        Accompany your journey with comfort
    </h1>
  );
}